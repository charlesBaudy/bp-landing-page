---
---
<section id="contact" class="contact-section" itemscope itemtype="http://schema.org/PaintingCompany">
  <div class="container">
    <h2>Contactez-moi</h2>
    <div class="contact-content">
      <div class="contact-info">
        <p itemprop="telephone">📱 06 20 15 64 10</p>
        <p itemprop="email">✉️ contact@baudy-peinture.com</p>
        <div class="business-hours" itemprop="openingHoursSpecification" itemscope itemtype="http://schema.org/OpeningHoursSpecification">
          <h3>Horaires d'ouverture :</h3>
          <ul>
            <li>Lundi - Vendredi : 8h - 18h</li>
            <li>Samedi : Sur rendez-vous</li>
            <li>Dimanche : Fermé</li>
          </ul>
        </div>
        <div class="service-area">
          <h3>Zone d'intervention :</h3>
          <ul>
            <li itemprop="areaServed">Toulon</li>
            <li itemprop="areaServed">La Seyne-sur-Mer</li>
            <li itemprop="areaServed">Six-Fours-les-Plages</li>
            <li itemprop="areaServed">Hyères</li>
            <li itemprop="areaServed">La Valette-du-Var</li>
          </ul>
        </div>
      </div>
      <form class="contact-form">
        <input type="text" placeholder="Votre nom" required />
        <input type="email" placeholder="Votre email" required />
        <input type="tel" placeholder="Votre téléphone" required />
        <textarea placeholder="Votre message (Décrivez votre projet)" rows="5" required></textarea>
        <button type="submit">Demander un devis gratuit</button>
      </form>
    </div>
  </div>
</section>

<style>
  .contact-section {
    padding: 4rem 0;
    background: rgb(var(--background));
  }
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }
  h2 {
    text-align: center;
    color: rgb(var(--primary));
    margin-bottom: 3rem;
    font-size: 2.5rem;
    font-weight: 700;
    position: relative;
  }
  h2::after {
    content: "";
    position: absolute;
    bottom: -0.5rem;
    left: 50%;
    transform: translateX(-50%);
    width: 100px;
    height: 4px;
    background: rgb(var(--accent));
    border-radius: 2px;
  }
  .contact-content {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 2rem;
  }
  .contact-info {
    padding: 1rem;
    font-size: 1.2rem;
  }
  .contact-info p {
    margin-bottom: 1rem;
  }
  .business-hours, .service-area {
    margin-top: 2rem;
  }
  .business-hours h3, .service-area h3 {
    color: rgb(var(--primary));
    margin-bottom: 1rem;
    font-size: 1.2rem;
  }
  .business-hours ul, .service-area ul {
    list-style: none;
    padding: 0;
  }
  .business-hours li, .service-area li {
    margin-bottom: 0.5rem;
  }
  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  input, textarea {
    padding: 0.8rem;
    border: 1px solid rgba(var(--primary), 0.2);
    border-radius: 4px;
    font-size: 1rem;
  }
  button {
    background: rgb(var(--accent));
    color: white;
    padding: 1rem;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    transition: opacity 0.2s;
    font-size: 1.1rem;
    font-weight: 600;
  }
  button:hover {
    opacity: 0.9;
  }
  @media (max-width: 768px) {
    .contact-content {
      grid-template-columns: 1fr;
    }
    h2 {
      font-size: 2rem;
    }
  }
</style>